services:
  - type: worker
    name: luokai-moltbook
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python luokai.py
    disk:
      name: memory
      mountPath: /data
      sizeGB: 1
